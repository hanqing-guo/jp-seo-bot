# enkiseojp.com SEO 体检报告 — 2026-06-15

*执行方式：Claude Code 定时任务 ｜ 生成时间：2026-06-15 UTC*

---

## 执行摘要

本次自动体检通过抓取 Yahoo! JAPAN 搜索结果监测 enkiseojp.com 的收录与排名。**本次体检数据全部无法获取**，原因：

1. **Yahoo! JAPAN 返回 HTTP 403**：反爬虫保护拦截自动请求，6 项搜索查询（1 条 site: + 5 条关键词）全部失败。
2. **enkiseojp.com 不在执行环境网络白名单**：`/api/index-status` 调用被网络策略阻断（`Host not in allowlist`）。

本报告如实记录执行状态，数值部分延用 2026-06-11 基线，无法确认变化。

---

## 1. 收录数（Yahoo! JAPAN `site:enkiseojp.com`）

| 指标 | 2026-06-11（基线） | 2026-06-15（本次） | 变化 |
|------|:-----------:|:-----------:|:----:|
| 已收录页数 | 7 页 | ❌ 无法获取（403） | 不可知 |
| 站点总页数 | 17 页 | — | — |
| 收录率 | 41% | — | — |

**失败原因**：Yahoo! JAPAN 对来自机器人 IP 的搜索请求返回 403 Forbidden，无法程序化读取收录结果。

---

## 2. 关键词排名（Yahoo! JAPAN P1，目标：出现在前 10）

| # | 关键词 | 2026-06-11（基线） | 2026-06-15（本次） | 变化 |
|---|--------|:---------:|:---------:|:----:|
| ① | メタディスクリプション 書き方 | 未入前 10 | ❌ 403 | 不可知 |
| ② | 内部リンク 貼り方 | 未入前 10 | ❌ 403 | 不可知 |
| ③ | 中小企業 SEO 自分で | 未入前 10 | ❌ 403 | 不可知 |
| ④ | AI SEO 記事 自動作成 | 未入前 10 | ❌ 403 | 不可知 |
| ⑤ | 格安 SEO 対策 おすすめ | 未入前 10 | ❌ 403 | 不可知 |

---

## 3. 收录监控 API（`/api/index-status`）

- **调用结果**：❌ 网络访问被拒（`Host not in allowlist: enkiseojp.com`）
- **GSC 接入状态**：无法验证（configured 字段未知）

---

## 4. 基线对比（2026-06-11 → 2026-06-15）

| 维度 | 2026-06-11 | 2026-06-15 | 状态 |
|------|-----------|-----------|------|
| Yahoo 收录 | 7 / 17 | 无法获取 | ⚠️ 数据缺失 |
| 5 关键词前 10 | 0 / 5 | 无法获取 | ⚠️ 数据缺失 |
| GSC 接入 | — | 无法验证 | ⚠️ 数据缺失 |

没有变化可以报告——**是因为数据根本无法取到，而非没有变化**。这两者需要区分。

---

## 5. 已知结构性问题（2026-06-01 架构审计结论，本次无新数据但问题依然存在）

这些是导致收录率仅 41%、5 个目标词均不排名的根本原因：

- 🔴 **CSR SPA 架构**：纯客户端渲染，Googlebot 渲染慢且不稳；AI 爬虫（GPTBot / ClaudeBot / PerplexityBot）基本看不见内容（只有空 `<div id="root">`）。
- 🔴 **public/ 为空**：无 `robots.txt`、无 `sitemap.xml`、无 `llms.txt`，搜索引擎没有明确入口。
- 🔴 **零结构化数据**：全站无 JSON-LD（Organization / Article / FAQPage），无法拿到富结果。
- 🔴 **AI 元数据被管线丢弃**：DeepSeek 已生成 `metaDescription / faq / relatedKeywords`，但 `parseArticle` 只保留 `title + markdown`，其余字段直接丢弃。
- 🔴 **文章无发布通道**：工具产的文章只能复制，没有对外独立 URL，搜索引擎无页面可索引。

---

## 6. 下一步建议

### 🔴 优先级 1：修复监控本身

1. **接入 Google Search Console API**（官方 OAuth）：替代爬取 Yahoo! JAPAN，获取权威收录/点击数据，稳定可靠无 403 风险。实施后定时任务可直接读 GSC 数据。
2. **将 enkiseojp.com 加入执行环境网络出口白名单**：使 `/api/index-status` 可访问，恢复本地 API 监控通路。

### 🟠 优先级 2：最高 ROI 的 SEO 改善（参照 6-01 审计）

3. **补 `robots.txt` + `sitemap.xml`**：public/ 一行配置都没有，这是收录率低的直接技术原因，改动 S 级。
4. **捡回 AI 已生成的 `metaDescription` + `faq`**：`parseArticle` 只需扩展 2 字段即可，DraftArticle 类型加 `metaDescription / faq / relatedKeywords`，直接改善 `<meta>` 和富结果，工作量 S。

### 🟡 优先级 3：架构方向决策

5. **决定文章发布站形态**：内置 SSG/SSR（每篇文章独立 URL + canonical + schema）或接 CMS（WordPress/Hatena 等）。这是收录率 41% 的根本瓶颈，「文章存在 localStorage 里」不会被索引。

---

## 7. 监控方案改进建议

当前定时任务依赖爬取 Yahoo! JAPAN 公开搜索页，存在以下系统性风险：

| 风险 | 说明 |
|------|------|
| 反爬虫封锁 | Yahoo! JAPAN 对机器人 IP 频繁返回 403，本次全部失败 |
| 非真实视角 | 结果受 IP 地域/Cookie 影响，非日本用户真实排名 |
| 不稳定 | 无 API SLA，随时可能失效 |

**推荐替代方案**：Google Search Console Data API（`searchanalytics.query`），官方支持，返回 Japan 维度的真实点击/曝光/排名，单次 API 调用覆盖全部关键词，完全避免 403 问题。

---

*下次体检建议：完成 GSC API 接入后再跑，届时可获得真实数据进行比对。*
