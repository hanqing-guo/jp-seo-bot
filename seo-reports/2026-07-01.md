# enkiseojp.com SEO 体检报告 — 2026-07-01

*执行方式：Claude Code 定时任务 ｜ 生成时间：2026-07-01 UTC*

---

## 执行摘要

本次自动体检**所有数据均无法获取**，与 2026-06-15 报告完全相同。这是**连续第 2 次监控失败**，属于结构性障碍，不是偶发故障。

失败原因：
1. **Yahoo! JAPAN 返回 HTTP 403**：反爬虫保护拦截自动请求，6 项搜索查询（1 条 `site:` + 5 条关键词）全部失败。
2. **执行环境代理阻断 enkiseojp.com**：`/api/index-status` 调用经由内部 HTTPS 代理，代理返回 `403 Forbidden — CONNECT tunnel failed`，enkiseojp.com 不在代理出口白名单内。

本报告如实记录执行状态，无法提供新增排名/收录数值。

---

## 1. 收录数（Yahoo! JAPAN `site:enkiseojp.com`）

| 指标 | 2026-06-11（基线） | 2026-06-15 | 2026-07-01（本次） | 变化 |
|------|:-----------:|:-----------:|:-----------:|:----:|
| 已收录页数 | 7 页 | ❌ 无法获取（403） | ❌ 无法获取（403） | 不可知 |
| 站点总页数 | 17 页 | — | — | — |
| 收录率 | 41% | — | — | — |

**注意**：连续 2 次数据缺失，Yahoo! JAPAN 反爬已成为监控常态性障碍。

---

## 2. 关键词排名（Yahoo! JAPAN，目标：出现在前 10）

| # | 关键词 | 2026-06-11（基线） | 2026-07-01（本次） | 变化 |
|---|--------|:---------:|:---------:|:----:|
| ① | メタディスクリプション 書き方 | 未入前 10 | ❌ 403 | 不可知 |
| ② | 内部リンク 貼り方 | 未入前 10 | ❌ 403 | 不可知 |
| ③ | 中小企業 SEO 自分で | 未入前 10 | ❌ 403 | 不可知 |
| ④ | AI SEO 記事 自動作成 | 未入前 10 | ❌ 403 | 不可知 |
| ⑤ | 格安 SEO 対策 おすすめ | 未入前 10 | ❌ 403 | 不可知 |

---

## 3. 收录监控 API（`/api/index-status`）

- **调用结果**：❌ 代理返回 403（`CONNECT tunnel failed — Host not in allowlist: enkiseojp.com`）
- **GSC 接入状态**：无法验证（configured 字段未知）

---

## 4. 基线对比（2026-06-11 → 2026-07-01）

| 维度 | 2026-06-11 | 2026-06-15 | 2026-07-01 | 趋势 |
|------|:----------:|:----------:|:----------:|:----:|
| Yahoo 收录 | 7 / 17 | ❌ 获取失败 | ❌ 获取失败 | ⚠️ 连续 2 次失败 |
| 5 词前 10 | 0 / 5 | ❌ 获取失败 | ❌ 获取失败 | ⚠️ 连续 2 次失败 |
| GSC API | — | ❌ 代理阻断 | ❌ 代理阻断 | ⚠️ 连续 2 次失败 |

连续两次失败意味着：**监控机制本身已经失效**，无论 SEO 状况如何变化都无法感知。

---

## 5. 已知结构性问题（SEO 本体，上次无变化，本次同）

以下问题自 2026-06-01 架构审计起已知，监控数据虽不可得，但问题本身未解决：

- 🔴 **CSR SPA 架构**：纯客户端渲染，Googlebot/AI 爬虫只看到空 `<div id="root">`。
- 🔴 **public/ 为空**：无 `robots.txt`、无 `sitemap.xml`、无 `llms.txt`。
- 🔴 **零结构化数据**：全站无 JSON-LD（Organization / Article / FAQPage）。
- 🔴 **AI 元数据丢弃**：`parseArticle` 只保留 `title + markdown`，`metaDescription / faq / relatedKeywords` 未写入页面。
- 🔴 **文章无独立 URL**：文章只存在 localStorage，无可索引的独立页面。

---

## 6. 下一步建议

### 🔴 最优先（1 件事）：修复监控通路

**接入 Google Search Console API（官方 OAuth）**，彻底替代爬取 Yahoo! JAPAN。

- 原因：Yahoo! JAPAN 反爬封锁已连续 2 次导致监控空转；GSC API 是官方数据源，返回真实的日本区收录/点击/排名数据，无 403 风险。
- 实施方式：在 Google Cloud Console 创建 Service Account → 授权到 GSC 属性 → 在定时任务里调用 `searchanalytics.query`（指定 country: JPN）。
- 工作量：约 2～4 小时配置，一次性解决所有监控问题。

### 🟠 优先级 2：最快见效的 SEO 改善

1. **补 `robots.txt` + `sitemap.xml`**：修改 `public/`，10 分钟内可完成，直接改善收录率。
2. **捡回 AI 已生成的 `metaDescription`**：`parseArticle` 扩展 2 字段，改善元标签和富结果展示。

### 🟡 优先级 3：架构决策

3. **决定文章发布站形态**（SSG / SSR / CMS）：文章不可索引是收录率 41% 的根本原因。

---

## 7. 监控方案风险总结

| 风险 | 说明 | 状态 |
|------|------|:----:|
| Yahoo! JAPAN 反爬封锁 | 机器人 IP 频繁 403 | 🔴 已成常态 |
| 代理出口白名单限制 | enkiseojp.com 不在允许列表 | 🔴 持续阻断 |
| 排名数据不稳定 | 无 API SLA，IP/Cookie 影响结果 | 🔴 架构缺陷 |

**推荐替代**：Google Search Console Data API（`searchanalytics.query`），Japan 维度，官方支持，稳定无反爬风险。

---

*【异常】标记：本次为连续第 2 次监控全部数据缺失，已在邮件草稿标题加前缀提醒。*

*下次体检建议：完成 GSC API 接入后再跑，届时可获得真实数据进行比对。*
